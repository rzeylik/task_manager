import React, {useState} from 'react'
import PropTypes from 'prop-types'

const CardActionsTitle = ({children}) => {
    return (
        <h6>
            { children }
        </h6>
    )

}

CardActionsTitle.propTypes = {}

export default CardActionsTitle
